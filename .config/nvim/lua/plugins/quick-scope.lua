return {
  "unblevable/quick-scope",
  -- im keeping always on (default) instead of highlight on keys
  -- init = function()
  --   vim.g.qs_highlight_on_keys = { "f", "F", "t", "T" }
  -- end,
}
